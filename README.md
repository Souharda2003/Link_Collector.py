# Link_Collector.py
A Flask-based web application that accepts a website URL, crawls the page using BeautifulSoup, extracts all valid HTTP/HTTPS links, and allows users to download the collected links instantly as a text file.*
